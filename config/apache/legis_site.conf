# Apache virtual host for the Legis 2025 portal.
# Replace the placeholders with the absolute paths and domains used on your server.

<VirtualHost *:80>
    ServerName elections.example.com
    ServerAdmin admin@example.com

    # Root of the Django project checkout on the server.
    DocumentRoot /var/www/elecciones-suigeneris

    # mod_wsgi daemon keeps the site isolated from other vhosts.
    WSGIDaemonProcess legis_site \
        python-home=/var/www/elecciones-suigeneris/.venv \
        python-path=/var/www/elecciones-suigeneris \
        processes=2 \
        threads=4
    WSGIProcessGroup legis_site
    WSGIApplicationGroup %{GLOBAL}
    WSGIScriptAlias / /var/www/elecciones-suigeneris/legis_site/wsgi.py

    # Static assets collected via `python manage.py collectstatic`.
    Alias /static/ /var/www/elecciones-suigeneris/static/
    <Directory /var/www/elecciones-suigeneris/static/>
        Require all granted
    </Directory>

    # Media (optional) — comment out if not used.
    Alias /media/ /var/www/elecciones-suigeneris/media/
    <Directory /var/www/elecciones-suigeneris/media/>
        Require all granted
    </Directory>

    <Directory /var/www/elecciones-suigeneris/legis_site>
        <Files wsgi.py>
            Require all granted
        </Files>
    </Directory>

    # Environment for the Django application.
    SetEnv DJANGO_SETTINGS_MODULE legis_site.settings
    SetEnv PYTHONUNBUFFERED 1

    ErrorLog ${APACHE_LOG_DIR}/legis_site_error.log
    CustomLog ${APACHE_LOG_DIR}/legis_site_access.log combined

    # Remove or restrict if server-status is not desired.
    <Location "/server-status">
        SetHandler server-status
        Require local
    </Location>
</VirtualHost>
